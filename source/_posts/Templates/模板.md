---
<%*
// 提示用户输入标题
const title = await tp.system.prompt("请输入文章标题");
if (!title) {
  // 如果用户取消输入，则删除此文件并停止
  const file = tp.file.find_tfile(tp.file.path(true));
  await app.vault.trash(file, true);
  return;
}
// 根据输入的标题和当前日期，生成新的文件名
const datePrefix = tp.date.now("YYYY-MM-DD");
const newFileName = `${datePrefix}-${title}`;
await tp.file.rename(newFileName);
-%>
title: <% title %>
date: <% tp.date.now("YYYY-MM-DD HH:mm:ss") %>
updated: <% tp.date.now("YYYY-MM-DD HH:mm:ss") %>
tags:
  - 
categories: 
keywords:
  - 
description:
top_img:
comments:
cover:
mathjax: true
---

# <% title %>

<% tp.file.cursor() %>